# Заготовки витрины

Каталог используется командой `php artisan shop:install`. После выбора стека нужная папка копируется в корень проекта, лишние фронты не подключаются.

В установленном проекте (без `.git` исходника) `stubs/` удаляется. В этом репозитории шаблоны хранятся постоянно.
